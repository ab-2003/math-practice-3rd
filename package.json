{
  "name": "math-practice-3rd",
  "private": true,
  "version": "0.20.0-alpha.6",
  "type": "module",
  "description": "TRICK LINE — a math fact fluency PWA that honours derivation while driving toward automaticity.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "playwright": "^1.49.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "jsqr": "^1.4.0",
    "qrcode-generator": "^2.0.4"
  }
}
